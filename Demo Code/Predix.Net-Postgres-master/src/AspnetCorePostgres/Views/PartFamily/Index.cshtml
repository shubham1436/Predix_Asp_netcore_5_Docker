@model IEnumerable<AspnetCorePostgres.Models.PartFamily>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New PartFamliy</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PartFamilyID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PartFamilyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PartFamilyID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PartFamilyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PartFamilyID">Edit</a> |              
                <a asp-action="Delete" asp-route-id="@item.PartFamilyID" onclick="return confirm('Are sure wants to delete?');">Delete</a>
            </td>
        </tr>
    }
</table>
